<li>
  &middot;

  {{#if (or (not isLong) isExpanded)}}
    {{comment.text}}

  {{else}}
    {{truncate comment.text 24}}...

  {{/if}}

  {{#if isLong}}
    <a href="#"
      {{action (toggle "isExpanded" this)}}
      class="gray">
        Show {{if isExpanded "less" "more"}}</a>
  {{/if}}

  <em class='i'>– {{comment.author}}</em>

</li>
