{{#ui-form onSubmit=(action "save") as |form|}}

  {{#form.label}}
    my label
  {{/form.label}}

  {{form.input
    value=name
    placeholder="my input"}}

  {{#ui-button onClick=(action (mut name) "")}}
    Reset
  {{/ui-button}}

  {{#form.submit}}
    save
  {{/form.submit}}

{{/ui-form}}
